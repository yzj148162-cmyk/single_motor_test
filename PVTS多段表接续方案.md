# PVTS多段表接续方案

## 1. 方案定位

当前程序界面中称为 `PVT` 的模式，实际底层调用的是：

- `dmc_pvts_table_unit`
- `dmc_pvt_move`

因此本方案的准确对象是：**PVTS 多段表顺序接续**。

本方案的目标不是控制卡内部连续流式装表，而是：

- 软件先生成整条全局轨迹
- 按段切成多个 `PVTS` 子表
- 每段最多 `5000` 点
- 当前段执行完成后，再由硬件线程立即装载并启动下一段

## 2. 已知约束

- 公开手册中，`PVTS` 单次装表容量上限为 `5000` 点
- 当前公开接口路线仍是：
  - `dmc_pvts_table_unit`
  - `dmc_pvt_move`
- 不依赖未文档化的 `pvt_continuous` 接口
- 不在当前段仍在运行时更新表
- 下一段正式装载条件使用“当前段 `done == true`”

## 3. 轨迹切段原则

### 3.1 全局轨迹先生成

- 规划线程先生成完整全局轨迹
- 时间轴在规划层保持全局连续

### 3.2 相邻段重叠 1 个边界点

示例：

- 第 1 段：`0..4999`
- 第 2 段：`4999..9998`
- 第 3 段：`9998..14997`

作用：

- 下一段首点继承上一段末点
- 下一段可以局部重基准为 `t = 0`、`pos = 0`
- 段间首末速度可以在规划意义上对齐

### 3.3 每段下发前做局部重基准

控制卡接收的每一段 `PVTS` 时间轴必须是局部时间轴。

处理方式：

- `localTime = point.timeS - segmentFirst.timeS`
- `localPos = point.pos - segmentFirst.pos`
- 首点强制为 `0`

UI 显示仍保持全局时间连续。

## 4. 当前程序中的接续触发时刻

当前程序不是提前预装后表，而是：

1. 硬件线程周期运行
2. 检测当前段 `done == true`
3. 若后面仍有下一段，则立即调用 `startPvtSegment(next)`
4. `startPvtSegment(next)` 内部再执行：
   - `dmc_pvts_table_unit`
   - `dmc_pvt_move`

因此它本质上是：**前段完成后，再软件接力启动后段**。

## 5. 已完成的程序改造

- UI 不再以 `5000` 点作为整条轨迹的硬限制
- 规划线程支持把整条全局轨迹切成多个 `PvtTrajectorySegment`
- 硬件线程支持按段启动与顺序接续
- 运行日志会输出：
  - 总点数
  - 分段数
  - 每段装载信息
  - 段间接续耗时

## 6. 接续耗时测量方法

已在硬件线程内加入测量埋点，测量点为：

- `T_done`：检测到前段完成
- `T_launch_begin`：开始调用下一段装表/启动
- `T_launch_end`：下一段装表/启动返回成功
- `T_motion_resume`：位置反馈重新出现稳定变化

日志中输出的主要量：

- `done->launchBegin`
- `launchBegin->launchEnd`
- `done->launchEnd`
- `done->motionResume`
- `launchEnd->motionResume`

其中：

- `done->launchEnd` 代表软件侧接续耗时
- `done->motionResume` 代表从反馈角度观察到的实际恢复耗时

## 7. 本轮验证结论

实测日志显示，多次段间接续结果基本稳定为：

- `done->launchEnd ≈ 59~60 ms`
- `launchEnd->motionResume ≈ 0.2~0.3 ms`

这说明：

- 大部分断续时间消耗在**下一段装表与重新启动**
- 电机重新响应本身不是主要瓶颈
- 反馈检测本身也不是主要瓶颈

因此可以明确得出结论：

**当前这条“前段 done 后，再通过 `dmc_pvts_table_unit + dmc_pvt_move` 软件接力启动下一段”的 PVTS 多段表接续方案，无法满足平滑连续接续要求。**

换句话说：

- 它可以用于“长轨迹分段跑完”
- **不适合用于“多段近似无缝连续执行”**

## 8. 本方案的保留价值

虽然该方案已不适合作为最终连续方案，但仍有保留价值：

- 验证了轨迹可以突破单段 `5000` 点限制
- 验证了段间切换的真实耗时量级
- 排除了“只是反馈慢”或“只是检测逻辑有误”的可能
- 为后续路线选择提供了明确依据

## 9. 后续建议路线

后续不建议继续在当前 `PVTS done 后接力装表` 路线上投入过多优化时间，建议转向：

### 路线 A：继续深挖连续接口

- 调研 `pvt_continuous` 相关接口
- 核实是否存在官方支持的长轨迹连续装表方案

### 路线 B：改用更适合连续轨迹的控制方式

- 回到 `CSP` 周期下发
- 或评估控制卡公开支持的其他缓冲型轨迹接口

## 10. 结论摘要

本方案已经完成实现与测量验证，最终结论是：

- **PVTS 多段表顺序接续可行**
- **PVTS 多段表平滑连续接续不可行**
- 主要瓶颈是段切换时的装表/启动耗时约 `60 ms`

