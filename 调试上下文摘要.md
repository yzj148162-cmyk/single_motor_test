# 调试上下文摘要

## 当前主线

- 当前主线只保留两部分：
  - `PVT(PVTS)` 分段接续改造
  - 单独按钮触发的“读取实际位置”与控制卡初始化联调
- 暂时**不再追踪中文乱码问题**，除非后续它直接影响编译或功能。

## 已完成的 PVT 改造

- 当前程序中的“PVT 模式”实际走的是 `PVTS`：
  - 使用 `dmc_pvts_table_unit`
  - 使用 `dmc_pvt_move`
- 已完成“超过 5000 点的分段接续”：
  - 规划线程先生成全局轨迹
  - 再切成多个 segment
  - 每段最多 `5000` 点
  - 相邻段重叠 `1` 个边界点
- 硬件线程已支持：
  - 首段装表启动
  - 当前段完成后切到下一段
  - 分段运行日志输出

## PVT 相关既定原则

- 每段下发给 `PVTS` 的局部时间必须从 `0` 开始
- 软件维护全局时间轴
- 段切换以“当前段完成后再启动下一段”为原则
- 当前实现目标是“软件接力式分段执行”
- 当前实现**不承诺**控制卡内部无缝流式续装

## 当前“读取实际位置”联调结论

- 初始化后空闲态高频读取已经关闭：
  - 硬件线程仅在 `motionActive_ == true` 时周期读反馈
  - 空闲态不再每 `1ms` 自动读位置
- `读取实际位置` 按钮当前走单次主动读取

## 当前接口判断

- 手册正文明确能查到：
  - `dmc_get_encoder_unit`
  - `dmc_set_equiv`
- 手册正文未查到：
  - `dmc_get_encoder_ex`
- 因此“读取实际位置”已从 `dmc_get_encoder_ex` 切回 `dmc_get_encoder_unit`

## 当前初始化判断

- 初始化路径：
  - `dmc_board_init`
  - 等待总线轴就绪
  - `dmc_set_equiv`
- 为避免“关闭后立即重新初始化”时轴尚未就绪，已补充轻量等待：
  - 轮询 `nmc_get_total_axes`
  - 确认目标轴号已可见后再下发 `dmc_set_equiv`

## 后续联调时优先关注

- `读取实际位置` 是否仍返回错误码
- `关闭控制卡 -> 再初始化` 是否仍卡在 `dmc_set_equiv`
- `PVT` 分段长轨迹是否按预期逐段切换

## 暂不纳入主线

- 中文乱码清洗
- 大范围日志文案整理
- 非 PVT 模式的界面细节优化
